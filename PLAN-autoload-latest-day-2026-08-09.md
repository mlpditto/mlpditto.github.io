# PLAN — v5: Auto-โหลดวันล่าสุดตอนเปิดหน้า (แทนหน้า "รอรับข้อมูล")

> วางแผน 2026-08-09 · ไฟล์: `public/Sales Analytics.html` (ปัจจุบัน V136.29 "Day Pills", live แล้ว)
> feedback ต่อจาก v4: "ต้อง paste ก่อนถึงแสดงข้อมูล? ไม่แสดงค้างไว้แบบใน LINE MAN?" — ยืนยันแล้วว่าอยากให้ auto-โหลดวันล่าสุดจาก cloud ทันทีหลัง login แทนหน้าว่าง ช่อง paste ยังใช้เพิ่มข้อมูลใหม่เหมือนเดิม (ไม่ได้ตัดออก)

## หลักการ

- ใช้ผลลัพธ์จาก `loadRecentDayPills()` (v4) ที่ query อยู่แล้ว — ไม่ต้อง query ซ้ำ แค่ต่อท้ายด้วยการเรียก `loadCloudData(docId)` ของวันล่าสุด
- **guard ด้วย `allData.length === 0`** ก่อน auto-load กันทับข้อมูลที่ผู้ใช้อาจกำลังวาง/ประมวลผลอยู่พอดีตอน query กำลังทำงาน (race condition edge case)
- ไม่แตะ `loadCloudData`/ปุ่ม paste/ปุ่ม Cloud เดิมเลย — เพิ่มแค่ 3 บรรทัดต่อท้าย `loadRecentDayPills`

## แก้ไข

**anchor:** ท้าย `loadRecentDayPills()` (v4) หลัง `renderRecentDayPills();` ในบล็อก `try` เพิ่ม:
```javascript
// auto-load วันล่าสุดถ้ายังไม่มีข้อมูลอยู่ในมือ (กันทับข้อมูลที่ผู้ใช้กำลังวาง/ประมวลผลอยู่)
if (allData.length === 0 && recentDayPillsCache.length > 0) {
    loadCloudData(recentDayPillsCache[0].id);
}
```

## ข้อควรระวัง

- ถ้าไม่มีข้อมูลบน cloud เลย (โปรเจกต์ใหม่/ยังไม่เคยบันทึก) → `recentDayPillsCache.length === 0` ไม่ auto-load อะไร ยังเห็นหน้า "รอรับข้อมูล" ตามปกติ ไม่ error
- เพิ่ม 1 Firestore read ต่อการเปิดหน้า (`loadCloudData` ดึงเอกสารเต็มอีกรอบ แม้ `loadRecentDayPills` จะดึงมาแล้วก็ตาม) — ยอมรับได้ ไม่คุ้มความซับซ้อนที่ต้องรีแฟกเตอร์ `loadCloudData` ให้รับข้อมูลที่ fetch มาแล้วแทนการ fetch ซ้ำ
- ผู้ใช้ยัง paste/ประมวลผลข้อมูลใหม่ทับได้ตามปกติทุกเมื่อ (ฟีเจอร์นี้แค่เปลี่ยนค่าเริ่มต้นตอนเปิดหน้า ไม่ได้ล็อกอะไร)

## Verify ที่บ้าน

1. เปิดหน้าเว็บใหม่ (ปิด tab เดิมจริงๆ ไม่ใช่ reload ที่ auth cache) → ข้อมูลวันล่าสุดขึ้นเองโดยไม่ต้องกดอะไร ปุ่มวันที่ตรงกันไฮไลต์ถูก
2. ลอง paste ข้อมูลใหม่ทันทีหลังหน้าโหลด (ก่อน auto-load เสร็จ) → ไม่มีข้อมูลหาย/ถูกทับผิดจังหวะ
3. ทดสอบตอนไม่มีข้อมูลบน cloud เลย (หรือ user ใหม่ไม่เคยบันทึก) → เห็นหน้า "รอรับข้อมูล" ตามปกติ ไม่ error

## เช็คก่อนเริ่ม

- [ ] อยู่บนไฟล์จริงที่บ้าน (`D:\20_Code`)
- [ ] backup เก็บที่ scratchpad เท่านั้น ห้ามเก็บใน `public/`
- [ ] deploy ตาม workflow ปกติ — push เฉพาะ "Go Online"
