# PLAN — แถบกระทบยอด (Reconcile Bar) Phase 1+2

> วางแผน 2026-08-12 · ไฟล์: `public/Sales Analytics.html` (ปัจจุบัน V136.34 "Cloud Menu") → บัมพ์เป็น **V136.35 "Reconcile"**
> ที่มา: วันที่ 11/08/2026 ยอดรวมแอปตรงกับรายงาน CW เป๊ะ (43,671.00 / 124 บิล) แต่รายช่องทางเพี้ยน — เงินสดขาด 240, โอนเกิน 660, และรายงานมีเช็ค 420 ที่แอปไม่มีช่องรับ (240 + 420 = 660 พอดี = บิลเดียวจ่าย 2 ทาง แต่แอปบันทึกเป็นโอนก้อนเดียว)
> ขอบเขตที่ user เคาะแล้ว: ทำ **Phase 1+2 รวมเป็นก้อนเดียว** · **ไม่เพิ่มช่องทาง "เช็ค"** (ร้านไม่รับเช็คทั้งปลีก-ส่ง)

---

## 1. หลักการ

- **ไม่แตะ logic เดิม**: `renderAll()` คำนวณยอดรายช่องทางครบอยู่แล้ว (`ga_c`, `ga_l`, `ga_f`, `ga_s`, `ga_cred`, `ga_ar` + count `g_*`) — แถบใหม่แค่ "อ่านผล" ตอนท้าย ไม่คำนวณซ้ำ ไม่แก้สูตรเดิมสักบรรทัด
- **เทียบระดับกลุ่ม ไม่ใช่รายแบงก์**: รายงาน CW เห็น "บัญชีธนาคาร" เป็นก้อนเดียว แยก LPM/FISCAL/SCB ไม่ได้ → บรรทัดโอน = `ga_l + ga_f + ga_s` เสมอ ถ้าเทียบรายแบงก์จะแดงตลอดโดยไม่ได้ผิดจริง
- **ต้องปิดยอดได้ 100%**: ทุกบาทในรายงานต้องลงบรรทัดใดบรรทัดหนึ่ง ช่องทางที่แอปไม่มี (เช็ค/บัตรกำนัล/ธนาณัติ/อื่นๆ) ยุบเป็นบรรทัด **"อื่นๆ (แอปไม่มีช่อง)"** ซึ่ง "ในระบบ" = 0 เสมอ → ผลต่างของทั้งแถบต้องรวมกันได้ 0 ถ้าไม่ 0 แปลว่า parse พลาดหรือยอดรวมไม่ตรงจริง
- **ไม่มีเช็คเป็นช่องทาง** ตามที่เคาะ แต่ยอดเช็คยังต้องแสดงในบรรทัด "อื่นๆ" — ไม่งั้นเลขไม่มีวันลงตัว
- **Phase 1+2 ไม่ยุ่งกับคลาวด์**: ค่าเป้าหมายเก็บใน memory ผูกกับวันที่ที่กำลังดู (ไฟล์นี้ไม่ได้ใช้ `localStorage` เลยแม้แต่ที่เดียว — ไม่ตั้งธรรมเนียมใหม่ในแพตช์นี้) การบันทึกถาวรเป็นงาน Phase 5

## 2. ข้อจำกัดที่ตัดสินไปแล้ว (ไม่ต้องถามซ้ำตอนลงมือ)

| เรื่อง | ทางที่เลือก | เหตุผล |
|---|---|---|
| ช่องทางเช็ค | ไม่เพิ่ม → ยุบใน "อื่นๆ" | ร้านไม่รับเช็ค (ยอด 420 ของ 11/08 จึงควรตามดูว่าคีย์อะไรมา) |
| Pocket | **ไม่นับเข้ายอดรายช่องทาง** แต่แสดงเป็น**บรรทัดแยก** และบวกกลับเฉพาะตอนตรวจยอดรวม | Pocket คือที่แขวนบิลที่สงสัย ยอดหลักควรสะอาด — แต่ CW ไม่รู้จัก Pocket และนับบิลพวกนี้เต็มจำนวน ถ้าซ่อนไปเลยจะเกิดผลต่างลึกลับที่แยกไม่ออกว่ามาจากคีย์ผิดหรือมาจากบิลที่เราแขวนเอง (ไม่มีสวิตช์) |
| หลายวันพร้อมกัน | ใช้ได้เฉพาะตอนแสดง **วันเดียว** | รายงาน CW เป็นรายวัน ถ้าหลายวันให้ปิดช่องกรอกแล้วขึ้นข้อความแทน |
| โหมดความเป็นส่วนตัว | เปิด privacy = ซ่อนแถบทั้งแถบ | ไม่งั้นตัวเลขจริงหลุดทั้งที่โหมดนั้นตั้งใจซ่อน |
| จำนวนบิล | เก็บ + แสดงคู่ยอด แต่ **ยังไม่ตีความ** | การสรุปว่า "มีบิลแยกชำระ" เป็นงาน Phase 3 |

## 3. งานที่ต้องแก้ (5 จุด)

### 3.1 accumulator เพิ่มใน `renderAll()`

**anchor:** loop `allData.forEach` บรรทัด ~2588 (ที่คิด `g_*` / `ga_*` อยู่แล้ว)

ต้องเพิ่ม 2 อย่าง เพราะของเดิมยังไม่พอ:

1. **ยอดฝั่ง Pocket รายช่องทาง** (`pk_cash`, `pk_trans`, `pk_cred`, `pk_ar` + count) — ของเดิมเก็บแค่ `pocketTotal` รวม ๆ ใช้กางรายช่องทางไม่ได้ ให้สะสมในกิ่ง `if (r.isExcluded)` ที่มีอยู่แล้ว (ใช้กับบรรทัด Pocket และการตรวจยอดรวม ไม่ใช่บวกเข้ายอดรายช่องทาง)
2. **จำนวนบิลโอนแบบไม่นับซ้ำ** (`g_transBills`) — `g_l + g_f + g_s` จะนับซ้ำถ้าบิลเดียวจ่าย 2 แบงก์ ทำให้เทียบ count กับ CW เพี้ยน ให้ `if (transSum > 0) g_transBills++`

### 3.2 HTML แถบกระทบยอด

**anchor:** แทรกหลัง `</div>` ปิด tab strip (บรรทัด 726) ก่อน `<div id="tableContainer">` (บรรทัด 729)

โครง: หัวแถบพับได้ (ค่าเริ่มต้น = พับ) + ตัวแถบ 5 บรรทัด

```
[▸ กระทบยอดกับรายงาน CW]  [ตรงทุกช่อง ✓ / ไม่ตรง 2 ช่อง]        [วางยอดจากรายงาน] [ล้าง]
──────────────────────────────────────────────────────────────
ช่องทาง            ในระบบ         ควรเป็น (กรอก)     ผลต่าง
เงินสด            6,676.00 (29)   [      6916.00]   −240.00  ▼
โอน (รวม 3 บัญชี)  29,883.00 (79)  [     29223.00]   +660.00  ▲
บัตรเครดิต         6,536.00 (4)    [      6536.00]        —   ✓
ลูกหนี้             576.00 (12)    [       576.00]        —   ✓
อื่นๆ (แอปไม่มีช่อง)     0.00 (0)   [       420.00]   −420.00  ▼
──────────────────────────────────────────────────────────────
▸ ใน Pocket (ไม่นับ)     0.00 (0)
──────────────────────────────────────────────────────────────
รวม (+Pocket)    43,671.00 (124)      43,671.00 (124)   0.00  ✓
```

- id: `reconcileBar`, `reconcileToggle`, `reconcileStatus`, `rcPocketRow`, และต่อบรรทัด `rc-sys-{key}`, `rc-target-{key}`, `rc-diff-{key}` โดย key = `cash | trans | credit | ar | other`
- บรรทัด "อื่นๆ" **ซ่อนเมื่อทั้งในระบบและเป้าหมายเป็น 0** (ปกติร้านไม่มียอดพวกนี้ ไม่ต้องให้รก)
- **บรรทัด Pocket ซ่อนเมื่อ Pocket ว่าง** เป็นบรรทัดอ่านอย่างเดียว ไม่มีช่องกรอก กดกางแล้วเห็นแยกช่องทาง (`pk_cash` / `pk_trans` / `pk_cred` / `pk_ar`) เพื่อเดาได้ว่าบิลที่แขวนไว้กระทบช่องไหน — **ยอดนี้ไม่ถูกบวกเข้าบรรทัดช่องทางด้านบน** ตามที่ตกลง
- บรรทัดรวมท้ายสุดคือด่านตรวจสำคัญ: ฝั่ง "ในระบบ" ต้องเป็น **ยอดรายช่องทางทั้งหมด + Pocket** เพราะ CW นับบิลใน Pocket ด้วย ถ้าไม่บวกกลับ บรรทัดรวมจะแดงทุกครั้งที่มีบิลแขวนอยู่ทั้งที่ไม่มีอะไรผิด
- ผลต่างบรรทัดรวมต้อง 0 ถ้าไม่ 0 = ยอดรวมทั้งวันไม่ตรงจริง (คนละปัญหากับจัดช่องผิด) ให้ขึ้นเตือนคนละสีกัน
- สี: 0 = เขียว, ขาด = แดง, เกิน = ส้ม, ไม่ได้กรอก = เทา `—`
- **จอ 375px**: บังคับ grid 4 คอลัมน์บน `sm:` ขึ้นไป และ **stack เป็นการ์ดต่อบรรทัด** ต่ำกว่านั้น (บทเรียนเดิม: ตัวเลขฟอนต์หนาล้นคอลัมน์แคบ) — ต้องเช็คด้วย Playwright headless ที่ 375px ก่อนปล่อย

### 3.3 JS ฟังก์ชันใหม่

```javascript
let reconcileTargets = {};   // { "11/08/2026": { cash: 6916, trans: 29223, credit: 6536, ar: 576, other: 420 } }
let reconcileCounts  = {};   // จำนวนบิลจากรายงาน (เก็บไว้ให้ Phase 3 ใช้ต่อ, Phase นี้แค่แสดง)
```

- `getReconcileDate()` — คืนวันเดียวจาก `allData` ถ้ามีวันเดียว, คืน `null` ถ้าหลายวัน/ไม่มีข้อมูล (ใช้ logic uniqueDates เดียวกับ `updateDateRange()` บรรทัด 2035 — **แยกออกมาเป็นฟังก์ชัน `getUniqueDates()` แล้วให้ทั้งสองที่เรียกใช้** ไม่ก็อปโค้ดซ้ำ)
  - **ระวัง**: `updateDateRange()` ของเดิมกรอง `!r.isExcluded` ทิ้งก่อนหาวันที่ ถ้าบิลใน Pocket เป็นของวันอื่น จะมองไม่เห็น — ฟังก์ชันที่แยกออกมาต้องรับพารามิเตอร์ว่าจะรวม Pocket ไหม แล้วฝั่งกระทบยอด **ต้องรวม** (ไม่งั้นล็อกวันเดียวหลุด)
- `updateReconcileBar(sys, pocket)` — รับยอดที่ `renderAll()` คิดเสร็จแล้ว แล้ววาดแถบ
- `onReconcileInput(key, el)` — เก็บค่าลง `reconcileTargets[date][key]` แล้ววาดใหม่ (ช่องว่าง = ลบ key ทิ้ง = ไม่ตรวจ)
- `clearReconcile()` / `toggleReconcileBar()` / `toggleReconcilePocketDetail()`

**anchor เรียกใช้:** ท้าย `renderAll()` หลัง loop `tabList.forEach` (บรรทัด ~2827) — จุดนี้ค่าครบและเป็น global ทั้งหมดแล้ว

```javascript
updateReconcileBar({
    cash:   { amt: ga_c,               cnt: g_c },
    trans:  { amt: ga_l + ga_f + ga_s, cnt: g_transBills },
    credit: { amt: ga_cred,            cnt: g_cred },
    ar:     { amt: ga_ar,              cnt: g_ar },
    other:  { amt: 0,                  cnt: 0 }
}, { cash: pk_cash, trans: pk_trans, credit: pk_cred, ar: pk_ar, total: pocketTotal, cnt: pocketCount });
```

> ระวัง: ต้องใช้ `ga_*` (ทั้งวัน ไม่โดน filter) **ห้ามใช้ `tc/tlp/tf/ts/tcr/tar`** เพราะชุดหลังคิดจาก `filteredData` = โดนช่องค้นหาและแท็บที่เลือกอยู่กรองมาแล้ว ถ้าใช้ผิดจะกลายเป็น "พิมพ์ค้นหาแล้วผลต่างเปลี่ยน"

### 3.4 Modal วางยอดจากรายงาน CW

**anchor:** วาง modal ใหม่ระดับ body ต่อจาก modal ย้ายประเภทการชำระเงิน (**ห้าม nest ในแถบ** — parent ที่ `display:none` ทำให้ลูกไม่ render)

- textarea + ปุ่มวิเคราะห์ + พื้นที่พรีวิวก่อนยืนยัน (โชว์ว่าจะเติมช่องไหนเท่าไร แล้วค่อยกด "ใช้ค่านี้")
- ปุ่ม "ใช้ตัวอย่าง" สำหรับทดสอบ (แนวเดียวกับ import modal ของ CKNC)

**parser:**

```
1. เงินสด(30)                6,916.00
   รับ: เงินสด (30)          6,916.00
   จ่าย (0)                      0.00
```

- จับเฉพาะบรรทัด **หัวข้อที่ขึ้นต้นด้วยลำดับ** `^\s*(\d+)\.\s*(.+?)\((\d+)\)\s*([\d,]+\.\d{2})\s*$` — บรรทัดลูก (`รับ:` / `จ่าย`) ไม่ขึ้นต้นด้วย `N.` จึงไม่ชนกันเอง
- **จับคู่ด้วยชื่อก่อน แล้วค่อย fallback เป็นลำดับ** (ลำดับข้อในรายงานอาจสลับได้ ชื่อไม่สลับ):

| ชื่อในรายงาน | ลงบรรทัด |
|---|---|
| เงินสด | `cash` |
| บัญชีธนาคาร | `trans` |
| บัตรเครดิต | `credit` |
| เครดิตการค้า | `ar` |
| เช็คธนาคาร, บัตรกำนัล, ธนาณัติ, อื่นๆ | `other` (บวกสะสม) |

- อ่าน `รวมทั้งหมด (124) 43,671.00` แยกอีกตัว แล้ว **ตรวจว่าผลบวก 8 ข้อ = ยอดรวม** ถ้าไม่เท่า → เตือน "อ่านรายงานไม่ครบ" และไม่ยอมเติมค่า (กันเติมเลขผิดเงียบ ๆ)
- ถ้ามีบรรทัด `จ่าย` ที่ไม่ใช่ 0 → เตือนว่ายังไม่เคยเจอเคสนี้ ให้ตรวจเอง (ปัจจุบันยอดหัวข้อ = รับ ล้วน ยังไม่มีข้อมูลว่ารายงานหักจ่ายให้แล้วหรือยัง)
- ชื่อไทยที่ก็อปมาจาก report viewer อาจติด non-breaking space / ตัวไฮไลต์ → normalize ด้วย `replace(/\s+/g,'')` ก่อนเทียบชื่อ

### 3.5 บัมพ์เวอร์ชัน

`<title>` บรรทัด 7 และป้ายเวอร์ชันบรรทัด 375 → **V136.35 "Reconcile"**

## 4. เกณฑ์ผ่าน (ทดสอบด้วยข้อมูลจริง 11/08/2026)

1. โหลดวันที่ 11/08/2026 → กางแถบ → เห็น "ในระบบ": เงินสด 6,676.00 (29), โอน 29,883.00 (79→เช็คว่านับซ้ำหรือไม่), บัตร 6,536.00 (4), ลูกหนี้ 576.00 (12)
2. วางข้อความรายงานทั้งก้อน → เติมครบ 5 ช่องอัตโนมัติ, บรรทัด "อื่นๆ" โผล่ขึ้นมาพร้อมยอด 420.00
3. ผลต่างต้องได้ **เงินสด −240.00 · โอน +660.00 · อื่นๆ −420.00 · บัตร/ลูกหนี้ ✓ · รวม 0.00**
4. **เคส Pocket**: เขี่ยบิลเงินสดสัก 1 ใบเข้า Pocket → บรรทัดเงินสดลดลงตามยอดนั้น (ผลต่างเงินสดแดงเพิ่ม), บรรทัด Pocket โผล่พร้อมยอด/จำนวน, กางแล้วเห็นว่าเป็นเงินสด, และ **บรรทัดรวมยังต้องเป็น 0.00** ← ข้อนี้คือหัวใจของดีไซน์ ถ้าบรรทัดรวมแดงแปลว่าลืมบวก Pocket กลับ
5. คืนบิลจาก Pocket → ทุกอย่างกลับไปเหมือนข้อ 3
6. พิมพ์ค้นหาในตาราง / สลับแท็บ → ตัวเลขในแถบ **ต้องไม่ขยับ**
7. โหลดข้อมูลหลายวัน → แถบขึ้นข้อความ "ใช้ได้เมื่อแสดงวันเดียว" และช่องกรอกถูกปิด (ทดสอบเคสที่บิลใน Pocket เป็นคนละวันกับบิลหลักด้วย ต้องล็อกเหมือนกัน)
8. เปิดโหมดความเป็นส่วนตัว → แถบหายทั้งแถบ
9. จอ 375px → ไม่มี horizontal overflow (จับภาพด้วย Playwright headless chromium ยืนยัน)
10. วางข้อความมั่ว ๆ → ขึ้น error ไม่เติมค่าอะไรเลย, ค่าที่กรอกไว้เดิมไม่หาย

## 5. ที่ยังไม่ทำในแพตช์นี้ (กันขอบเขตบาน)

- ตีความ count ว่ามีบิลแยกชำระ (รายงานนับ 125 แต่หัวบอก 124) → **Phase 3**
- ปุ่มหาบิลผู้ต้องสงสัย + prefill `openTransferModal` → **Phase 4**
- บันทึกยอดเป้าหมาย/สถานะกระทบยอดขึ้นคลาวด์ + ติด ✓ บน day pills → **Phase 5**
- ช่องทาง "เช็ค" เต็มรูปแบบ → **ตัดทิ้งแล้ว** (ร้านไม่รับเช็ค)

## 6. Deploy

บัมพ์เวอร์ชัน → ทดสอบ local → `git push` **แล้วต้อง** `firebase deploy --only hosting` (push อย่างเดียวไม่ขึ้น live)
ห้ามสร้างไฟล์ `.bak` ใน `public/` เด็ดขาด (เคยหลุดขึ้น production มาแล้ว) — ไฟล์ชั่วคราวไปไว้ scratchpad
