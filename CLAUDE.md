# FKB-Front-Kanban

Static Firebase-hosted app; main surface is `public/cknc.html|css|js` (CLICKNIC × MEDLIFE PLUS × Billing Note reconciliation).

**Read [AGENT-PLAYBOOK.md](AGENT-PLAYBOOK.md) before working.** It defines the required
working loop (ground in code → propose → verify empirically), the two-layer CSS / dark-mode
/ event-delegation pitfalls, mock-data recipes for the analyze view, the user's
communication contract (Thai, propose→approve, lean UI), and the "Go Online" deploy flow.
